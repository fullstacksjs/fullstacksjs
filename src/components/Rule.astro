---
interface Props {
  target: string;
}

const { target } = Astro.props;
---

<li
  id={target}
  class="group ms-6 mb-3 leading-tight relative cursor-pointer transition-[color,opacity] motion-reduce:transition-none"
>
  <a class="cancel absolute inset-0 group-target:inline" href="#_"></a>
  <a class="text-accent-1 block w-ful" href={`#${target}`}>
    <slot />
  </a>
</li>

<style>
  li {
    list-style: '•  ';
  }

  .cancel {
    display: none;
  }

  li:target .cancel {
    display: inline;
  }

  li:has(~ li:target),
  li:target ~ li {
    opacity: 0.1;
  }

  li:not(:target):hover {
    opacity: 1;
  }

  li:not(:target):hover a {
    color: var(--fg-0);
  }
</style>
